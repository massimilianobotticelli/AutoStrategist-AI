# This is a Databricks asset bundle definition for AutoStrategist-AI.
# The Databricks extension requires databricks.yml configuration file.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.

bundle:
  name: AutoStrategist-AI-local

include:
  # - ./resources/ingestion.yml
  - ./resources/app.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-95e4ad93-dd1a.cloud.databricks.com/

  ## Optionally, there could be 'staging' or 'prod' targets here.
  #
  # prod:
  #   workspace:
  #     host: <YOUR_DATABRICKS_WORKSPACE_URL>
